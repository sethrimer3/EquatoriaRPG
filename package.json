{
  "name": "equatoria-idle",
  "version": "0.1.0",
  "description": "Equatoria Idle — a mobile-first idle game with equation progression and gem particles",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:desktop": "tsc --noEmit && vite build --mode desktop && node scripts/copy-assets.cjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "electron": "electron .",
    "desktop": "npm run build:desktop && electron .",
    "preview": "vite preview",
    "postbuild": "node scripts/copy-assets.cjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sethrimer3/Equatoria_Idle.git"
  },
  "keywords": [
    "idle",
    "game",
    "math",
    "equation"
  ],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sethrimer3/Equatoria_Idle/issues"
  },
  "homepage": "https://github.com/sethrimer3/Equatoria_Idle#readme",
  "devDependencies": {
    "electron": "^39.2.7",
    "eslint": "^10.2.0",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.2",
    "vite": "^8.0.3"
  }
}
